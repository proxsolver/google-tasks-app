# Google Tasks 매트릭스 앱 - 사용설명서

아이젠하워 매트릭스를 활용하여 Google Tasks를 효율적으로 관리할 수 있는 웹 애플리케이션입니다.

---

## 목차

1. [앱 소개](#앱-소개)
2. [시작하기](#시작하기)
3. [Google OAuth 설정](#google-oauth-설정)
4. [주요 기능](#주요-기능)
5. [화면별 가이드](#화면별-가이드)
6. [자주 묻는 질문](#자주-묻는-질문)

---

## 앱 소개

### 什么是 Google Tasks 매트릭스 앱?

Google Tasks와 연동되는 태스크 관리 앱으로, 아이젠하워 매트릭스(긴급/중요 4분면)와 마인드맵 뷰를 제공합니다.

### 주요 특징

- ✅ **Google Tasks 실시간 동기화** - 모든 변경사항이 즉시 Google Tasks에 반영
- ✅ **아이젠하워 매트릭스** - 긴급/중요 기준으로 태스크 분류
- ✅ **마인드맵 뷰** - 태스크를 계층형 트리로 시각화
- ✅ **태그 시스템** - 태스크에 태그를 지정하여 카테고리化管理
- ✅ **드래그 앤 드롭** - 분면 간 태스크 이동 지원

---

## 시작하기

### 1. 앱 실행

```bash
# 개발 서버 시작
npm run dev

# 프로덕션 빌드
npm run build
npm start
```

앱이 실행되면 http://localhost:3000에서 접속할 수 있습니다.

### 2. 초기 설정

#### 데이터베이스 설정

1. PostgreSQL 데이터베이스를 설치하고 실행합니다.
2. `.env.local` 파일에 `DATABASE_URL`을 설정합니다.

```bash
# .env.local 예시
DATABASE_URL=postgresql://postgres:password@localhost:5432/google_tasks
```

3. 데이터베이스 마이그레이션을 실행합니다.

```bash
npm run prisma:migrate
```

---

## Google OAuth 설정

Google Tasks API를 사용하려면 Google OAuth 2.0 설정이 필요합니다.

### 단계별 설정 가이드

#### 1. Google Cloud Console에서 프로젝트 생성

1. [Google Cloud Console](https://console.cloud.google.com/)에 접속
2. 상단의 프로젝트 선택 드롭다운에서 **"새 프로젝트"** 클릭
3. 프로젝트 이름 입력 (예: "Google Tasks Matrix")
4. **"만들기"** 클릭

#### 2. Tasks API 활성화

1. **"API 및 서비스"** > **"라이브러리"** 클릭
2. 검색창에 **"Google Tasks API"** 입력
3. **"Google Tasks API"** 클릭 후 **"사용"** 클릭

#### 3. OAuth 동의 화면 구성

1. **"API 및 서비스"** > **"사용자 인증 정보"** 클릭
2. **"OAuth 클라이언트 ID 만들기"** 클릭
3. 애플리케이션 유형: **"웹 애플리케이션"**
4. 다음 정보 입력:
   - 이름: Google Tasks Matrix
   - 승인된 자바스크립트 원본: `http://localhost:3000`
   - 승인된 리디렉션 URI: `http://localhost:3000/api/auth/callback/google`

#### 4. OAuth 2.0 클라이언트 ID 생성

1. **"만들기"** 클릭
2. 생성된 **클라이언트 ID**와 **클라이언트 보안 비밀**을 복사

#### 5. .env.local 파일 설정

```bash
# .env.local
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
NEXTAUTH_SECRET=openssl-rand-base64-32로-생성된-값
NEXTAUTH_URL=http://localhost:3000
DATABASE_URL=postgresql://user:password@localhost:5432/google_tasks
```

`NEXTAUTH_SECRET` 생성 방법:
```bash
openssl rand -base64 32
```

---

## 주요 기능

### 1. Google 로그인

**/login** 페이지에서 Google 계정으로 로그인합니다.

- Google OAuth 2.0을 사용한 안전한 인증
- 첫 로그인 시 자동으로 사용자 정보 저장
- Google Tasks API 접근 권한 요청

### 2. 태스크 관리

#### 태스크 생성

1. **"새 태스크"** 버튼 클릭
2. 다음 정보 입력:
   - **제목** (필수)
   - **메모** (선택)
   - **마감일** (선택)
   - **긴급** 체크박스
   - **중요** 체크박스
   - **태그** (선택)

#### 태스크 수정/삭제

- **수정**: 태스크 카드의 수정 아이콘 클릭
- **삭제**: 태스크 카드의 삭제 아이콘 클릭
- **완료 토글**: 태스크 카드의 체크박스 클릭

### 3. 아이젠하워 매트릭스

태스크가 **긴급/중요** 기준에 따라 4개 분면으로 자동 분류됩니다.

| 분면 | 긴급도 | 중요도 | 설명 |
|------|--------|--------|------|
| Q1 | ✅ 긴급 | ✅ 중요 | 즉시 처리해야 할 태스크 |
| Q2 | ❌ 비긴급 | ✅ 중요 | 계획적으로 처리할 태스크 |
| Q3 | ✅ 긴급 | ❌ 비중요 | 위임 가능한 태스크 |
| Q4 | ❌ 비긴급 | ❌ 비중요 | 나중에 처리할 태스크 |

#### 드래그 앤 드롭으로 분면 이동

태스크를 다른 분면으로 드래그하면 **긴급/중요** 정보가 자동으로 업데이트됩니다.

### 4. 마인드맵 뷰

**"마인드맵"** 버튼을 클릭하여 태스크를 계층형 트리 구조로 확인합니다.

- **확대/축소**: 마우스 휠 또는 줌 버튼
- **패닝**: 마우스 드래그
- **노드 상세**: 각 노드에 태스크 정보 표시

### 5. 태그 관리

**태그 아이콘**을 클릭하여 태그 관리 패널을 엽니다.

#### 태그 생성

1. **"새 태그"** 버튼 클릭
2. 태그 이름 입력
3. 색상 선택
4. **"생성"** 클릭

#### 태그 할당

태스크 생성/수정 시 태그를 선택하여 할당할 수 있습니다.

---

## 화면별 가이드

### 로그인 화면 (`/login`)

```
┌─────────────────────────────────┐
│  🔵 Google Tasks 매트릭스       │
│  아이젠하워 매트릭스로           │
│  효율적으로 태스크를 관리하세요   │
│                                 │
│  [로딩 중...]                   │
│                                 │
│  [Google로 로그인]              │
│                                 │
│  Google 계정으로 로그인하면      │
│  Google Tasks와 동기화됩니다     │
└─────────────────────────────────┘
```

- 자동으로 Google 로그인 페이지로 리디렉션
- 로그인 성공 시 `/dashboard`로 이동

### 대시보드 (`/dashboard`)

```
┌─────────────────────────────────────────────────────────┐
│ Google Tasks 매트릭스        12개 태스크  [+새 태스크]    │
│ [매트릭스] [마인드맵] [#] [새로고침] [로그아웃]          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┬─────────────┐                         │
│  │   Q1: 긴급+중요  │   Q2: 비긴급+중요 │                       │
│  │   즉시 처리     │   계획적 처리   │                       │
│  │   (3개)        │   (2개)        │                       │
│  ├─────────────┼─────────────┤                       │
│  │   Q3: 긴급+비중요 │  Q4: 비긴급+비중요 │                     │
│  │   위임 가능     │   나중에 처리   │                       │
│  │   (1개)        │   (0개)        │                       │
│  └─────────────┴─────────────┘                       │
└─────────────────────────────────────────────────────────┘
```

### 태스크 카드

```
┌─────────────────────────────────────┐
│ ☐  태스크 제목               [✏️][🗑️] │
│    메모 내용...                      │
│    📅 오늘                           │
│    🔥 긴급  ⭐ 중요  #업무 #개인     │
└─────────────────────────────────────┘
```

- **☐**: 완료 체크박스
- **✏️**: 수정 버튼
- **🗑️**: 삭제 버튼
- **📅**: 마감일 표시
- **태그**: 할당된 태그 목록

---

## 자주 묻는 질문

### Q: 데이터는 어디에 저장되나요?

**A:** Google Tasks API를 통해 Google 서버에 저장됩니다. 태그 정보만 로컬 PostgreSQL 데이터베이스에 저장됩니다.

### Q: 오프라인에서도 사용할 수 있나요?

**A:** 아니요, 이 앱은 Google Tasks API와 실시간 동기화되므로 인터넷 연결이 필요합니다.

### Q: 태스크가 Google Tasks 앱에서도 보이나요?

**A:** 네, 이 앱에서 생성/수정한 모든 태스크는 Google Tasks 앱에서도 확인할 수 있습니다.

### Q: 완료된 태스크는 어디서 확인하나요?

**A:** 완료된 태스크는 기본적으로 숨겨집니다. Google Tasks 앱에서 완료된 태스크를 확인할 수 있습니다.

### Q: 태스크를 실수로 삭제했는데 복구할 수 있나요?

**A:** 삭제된 태스크는 Google Tasks에서도 삭제되므로 복구할 수 없습니다. 삭제 전에 확인하시기 바랍니다.

### Q: 여러 Google 계정을 사용할 수 있나요?

**A:** 현재는 하나의 Google 계정만 지원합니다. 다른 계정을 사용하려면 로그아웃 후 다시 로그인해주세요.

### Q: 배포하려면 어떻게 하나요?

**A:** README.md의 배포 섹션을 참조하세요. Vercel에 쉽게 배포할 수 있습니다.

---

## 문제 해결

### 로그인이 안 될 때

1. `.env.local` 파일에 `GOOGLE_CLIENT_ID`와 `GOOGLE_CLIENT_SECRET`가 올바르게 설정되었는지 확인하세요.
2. Google Cloud Console에서 리디렉션 URI가 올바르게 설정되었는지 확인하세요.
3. Tasks API가 활성화되었는지 확인하세요.

### 데이터베이스 오류가 발생할 때

1. PostgreSQL이 실행 중인지 확인하세요.
2. `DATABASE_URL`이 올바른지 확인하세요.
3. 마이그레이션을 실행했는지 확인하세요:
   ```bash
   npm run prisma:migrate
   ```

### 태스크가 동기화되지 않을 때

1. **"새로고침"** 버튼을 클릭하세요.
2. 브라우저를 새로고침하세요 (F5).
3. 로그아웃 후 다시 로그인하세요.

---

## 추가 도움

- **GitHub Issues**: 버그 신고 및 기능 요청
- **README.md**: 프로젝트 설정 및 개발 정보

---

**즐거운 태스크 관리 되세요! 🎉**
